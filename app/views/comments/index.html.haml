%p#notice= notice
%h1 Список комментариев
%table.table_S
  %thead
    %tr
      %th Комментарий
      %th Дата
      %th{:colspan => "3"} Редактировать
  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.text
        %td= comment.date
        %td= link_to 'Show', comment
        %td= link_to 'Edit', edit_comment_path(comment)
        %td= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' }
%br
  = link_to '+ Создать комментарий', new_comment_path, class: 'button_S'
